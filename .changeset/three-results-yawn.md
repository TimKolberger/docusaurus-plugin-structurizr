---
'docusaurus-plugin-structurizr': major
---

Remove the deprecated Structurizr CLI executor and simplify executor handling. Docker is now the
only supported executor, so the executor plugin option has been removed
